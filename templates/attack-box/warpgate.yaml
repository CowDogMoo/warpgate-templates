# Warpgate Template: attack-box
# Converted from Packer HCL template
# Generated by: warpgate convert packer

metadata:
    name: attack-box
    version: 1.0.0
    description: This repository contains Packer templates to build **Attack-Box**
    author: Jayson Grace <jayson.e.grace@gmail.com>
    license: MIT
    tags:
        - security
        - red-team
    requires:
        warpgate: '>=1.0.0'
name: attack-box
version: latest
base:
    image: kalilinux/kali-last-release:latest
    pull: true
provisioners:
    - type: shell
      inline:
        - echo 'debconf debconf/frontend select Noninteractive' | debconf-set-selections
        - apt-get update
        - apt-get install -y python3 python3-pip sudo
    - type: ansible
      playbook_path: ${PROVISION_REPO_PATH}/playbooks/attack_box/attack_box.yml
      galaxy_file: ${PROVISION_REPO_PATH}/requirements.yml
      extra_vars:
        ansible_shell_executable: /bin/bash
    - type: shell
      inline:
        - apt-get clean
        - rm -rf /var/lib/apt/lists/* /tmp/* /var/tmp/*
targets:
    - type: container
      platforms:
        - linux/amd64
        - linux/arm64
      tags:
        - latest
    - type: ami
      region: us-east-1
      instance_type: t3.micro
      volume_size: 50
